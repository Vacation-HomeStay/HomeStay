import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SpecificResidence = (residence) => {
	return (
		<View>
			<Text>SpecificResidence</Text>
		</View>
	);
};

export default SpecificResidence;

const styles = StyleSheet.create({});
